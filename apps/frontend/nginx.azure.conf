server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Proxy API REST vers le backend AdonisJS
    location ^~ /api/ {
        proxy_pass https://${BACKEND_URL};
        proxy_ssl_server_name on;
        proxy_http_version 1.1;

        proxy_set_header Host ${BACKEND_URL};
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Proxy WebSocket (Socket.io) vers le backend
    location /socket.io/ {
        proxy_pass https://${BACKEND_URL};
        proxy_ssl_server_name on;
        proxy_http_version 1.1;

        proxy_set_header Host ${BACKEND_URL};
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket requis par Socket.io
        # Valeur Upgrade explicitement fixée à websocket
        # pour éviter le H2C smuggling.
        # nosemgrep: generic.nginx.security.possible-h2c-smuggling.possible-nginx-h2c-smuggling
        proxy_set_header Upgrade websocket;

        # nosemgrep: generic.nginx.security.possible-h2c-smuggling.possible-nginx-h2c-smuggling
        proxy_set_header Connection "upgrade";
    }

    location /health {
        return 200 'OK';
        add_header Content-Type text/plain;
    }

    location /read {
        return 200 'OK';
        add_header Content-Type text/plain;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}